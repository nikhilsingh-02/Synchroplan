/**
 * Optional LLM Enhancement Layer
 *
 * This service takes the structured insights generated by the rule-based Engine
 * and (optionally) rewrites them into natural language using an LLM.
 *
 * Currently unimplemented — acts as a pass-through stub.
 */

import type { AIInsight } from './optimization.engine';

export interface LLMConfig {
  enabled: boolean;
  apiKey?: string;
  provider?: 'openai' | 'anthropic' | 'gemini';
}

/**
 * Optionally enhances the provided insight using natural language generation.
 *
 * If the LLM is disabled or fails, it safely falls back to returning the original insight.
 */
export async function enhanceInsightWithLLM(
  insight: AIInsight,
  config: LLMConfig = { enabled: false }
): Promise<AIInsight> {
  if (!config.enabled || !config.apiKey) {
    return insight;
  }

  console.log(`[llm.service] Would enhance insight "${insight.title}" using ${config.provider}`);
  
  // TODO: Implement actual LLM call here (e.g. OpenAI SDK)
  //
  // const prompt = `
  //   Rewrite this scheduling insight into a friendly, helpful 1-sentence tip:
  //   Title: ${insight.title}
  //   Description: ${insight.description}
  //   Action: ${insight.recommendedAction}
  // `;
  //
  // try {
  //   const response = await fetchLLM(prompt);
  //   return { ...insight, description: response };
  // } catch {
  //   return insight; // Fallback
  // }

  return insight;
}
