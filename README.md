
  # Intelligent Schedule Optimization System

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
make sure to update the file time to time.
